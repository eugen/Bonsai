Add support for calling existing .NET classes
- better matching between the types of the arguments and a method's parameters
- should indexers have special syntax? 
- generics

Add OOP-ish behaviour through prototyping
 - having an "object" kind of function, with state and methods
 - support for cloning it

Null Value?